<!DOCTYPE html>
<html>
	<head>
  		<title><%= full_title(yield(:title)) %></title> <!-- uses helper function 'full_title' to dynamically assign each page a title; this helper function is defined in the app/helpers/application_helper.rb ruby file -->

  		<%= stylesheet_link_tag    "application", media: "all" %>
  		<%= javascript_include_tag "application" %>
  		<%= yield :javascript_includes %>
 		<%= csrf_meta_tags %>
		<%= render 'layouts/shim' %>	<!-- uses the Rails helper RENDER to look up the partial for the IE shim (located in app/views/layouts/_shimm.html.erb) -->

	</head>
	<body>
		<div id="wrap">
			<%= render 'layouts/header' %>
			<%= yield %>
			<div id="push"></div>
		</div>
			<%= render 'layouts/footer' %>
	</body>
</html>